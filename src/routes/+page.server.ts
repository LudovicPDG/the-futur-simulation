import hypothesis from '$lib/simulation/element/Hypothesis';

export async function load() {
	let hypothesis1 = new hypothesis(1, 'Hypothesis 1', 'Description of Hypothesis 1', 42, 0.9, new Set(), new Set(), new Set());
}
